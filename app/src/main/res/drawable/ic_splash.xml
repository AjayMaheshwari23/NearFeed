<?xml version="1.0" encoding="utf-8"?>
<!-- Native splash: black background with the NEAR-FEED mark centered.
     Centering uses gravity (not an <inset>), because an inset wrapper reports
     non-zero padding to the DecorView's window background, leaking a 40dp
     letterbox into the app content (squeezed the whole UI on cutout phones).
     Dismisses as soon as the Activity draws — no artificial delay. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:gravity="center"
        android:drawable="@drawable/ic_nearfeed_mark" />
</layer-list>
